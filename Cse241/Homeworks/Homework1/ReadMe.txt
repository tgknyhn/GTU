To execute game write `make` on terminal. 

After someone wins, game terminates.

To execute game again first write `make clean` to delete .o files and executable file.

otherwise `make` will tell `executable file is up to date`.  
